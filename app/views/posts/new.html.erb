<h1>New Post</h1>
<div class="posts">
  <h1>Create New Post</h1>
  <%= form_with(model: [@user, @post], url: user_posts_path(@user)) do |f| %>
    <div>
      <div class="post_label">
        <%= f.label :title %>
        <%= f.text_field :title, class: "post_input", required: true %>
      </div>
      <div class="post_label">
        <%= f.label :text %>
        <%= f.text_area :text, class: "post_input", rows: 3, cols: 25, required: true %>
      </div>
      <div class="btn">
        <p><%= f.submit "Add Post", class: "center_button"%></p>
      </div>
    </div>
  <% end %>
</div>
